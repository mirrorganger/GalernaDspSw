services:
  galerna-build:
    image: galerna-stm32-build:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/workspace
      - /dev/bus/usb:/dev/bus/usb
    working_dir: /workspace
    command: bash
    tty: true
    stdin_open: true
    privileged: true